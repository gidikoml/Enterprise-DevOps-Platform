apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enterprise-platform.fullname" . }}-test-connection"
  labels:
    {{- include "enterprise-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enterprise-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
