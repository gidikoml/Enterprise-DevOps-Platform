apiVersion: v1
kind: Service
metadata:
  name: {{ include "enterprise-platform.fullname" . }}
  labels:
    {{- include "enterprise-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enterprise-platform.selectorLabels" . | nindent 4 }}
